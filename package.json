{
  "name": "renewalradar",
  "version": "1.2.0",
  "private": true,
  "description": "RenewalRadar full-stack starter with authentication and responsive frontend.",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "linkedom": "^0.18.13"
  },
  "dependencies": {
    "stripe": "^22.6.1"
  }
}
