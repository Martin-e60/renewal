{
  "name": "duedar",
  "version": "1.3.2",
  "private": true,
  "description": "Duedar full-stack starter with authentication and responsive frontend.",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "linkedom": "^0.18.13"
  },
  "dependencies": {
    "libsql": "^0.5.29",
    "stripe": "^22.6.1"
  }
}
